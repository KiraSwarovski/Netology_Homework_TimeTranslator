package TaxType;

import java.math.BigDecimal;

public class ProgressiveTaxType extends TaxType{
//    @Override
//    public BigDecimal calculateTaxFor(BigDecimal amount) {
//        BigDecimal progressiveTaxType = BigDecimal.valueOf(amount.compareTo(BigDecimal.valueOf(100_000.00)))?BigDecimal.valueOf(0.15):BigDecimal.valueOf(0.1);
//        BigDecimal multiply = amount.multiply(BigDecimal.valueOf(progressiveTaxType));
//        return multiply;
//    }
}
